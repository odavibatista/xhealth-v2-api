import { BrowseTrainersUsecase } from './browse-trainers.usecase';

describe('BrowseTrainersUsecase', () => {
  it('should be defined', () => {
    expect(new BrowseTrainersUsecase()).toBeDefined();
  });
});
