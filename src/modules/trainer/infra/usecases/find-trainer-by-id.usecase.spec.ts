import { FindTrainerByIdUsecase } from './find-trainer-by-id.usecase';

describe('FindTrainerByIdUsecase', () => {
  it('should be defined', () => {
    expect(new FindTrainerByIdUsecase()).toBeDefined();
  });
});
