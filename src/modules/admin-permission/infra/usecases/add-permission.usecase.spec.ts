import { AddPermissionUsecase } from './add-permission.usecase';

describe('AddPermissionUsecase', () => {
  it('should be defined', () => {
    expect(new AddPermissionUsecase()).toBeDefined();
  });
});
