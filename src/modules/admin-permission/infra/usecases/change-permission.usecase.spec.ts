import { ChangePermissionUsecase } from './change-permission.usecase';

describe('ChangePermissionUsecase', () => {
  it('should be defined', () => {
    expect(new ChangePermissionUsecase()).toBeDefined();
  });
});
