import { RemovePermissionUsecase } from './remove-permission.usecase';

describe('RemovePermissionUsecase', () => {
  it('should be defined', () => {
    expect(new RemovePermissionUsecase()).toBeDefined();
  });
});
