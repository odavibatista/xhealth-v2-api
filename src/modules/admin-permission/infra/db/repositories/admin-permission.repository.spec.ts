import { AdminPermissionRepository } from './admin-permission.repository';

describe('AdminPermissionRepository', () => {
  it('should be defined', () => {
    expect(new AdminPermissionRepository()).toBeDefined();
  });
});
