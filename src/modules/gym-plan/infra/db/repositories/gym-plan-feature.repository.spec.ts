import { GymPlanFeatureRepository } from './gym-plan-feature.repository';

describe('GymPlanFeatureRepository', () => {
  it('should be defined', () => {
    expect(new GymPlanFeatureRepository()).toBeDefined();
  });
});
