import { ExtraServiceControllerDto } from './extra-service.controller.dto';

describe('ExtraServiceControllerDto', () => {
  it('should be defined', () => {
    expect(new ExtraServiceControllerDto()).toBeDefined();
  });
});
