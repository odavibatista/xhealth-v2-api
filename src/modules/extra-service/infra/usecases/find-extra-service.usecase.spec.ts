import { FindExtraServiceUsecase } from './find-extra-service.usecase';

describe('FindExtraServiceUsecase', () => {
  it('should be defined', () => {
    expect(new FindExtraServiceUsecase()).toBeDefined();
  });
});
