import { HealthControllerDto } from './health.controller.dto';

describe('HealthControllerDto', () => {
  it('should be defined', () => {
    expect(new HealthControllerDto()).toBeDefined();
  });
});
